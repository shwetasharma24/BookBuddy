import React from "react";
import ReactLoading from "react-loading";

export default () => (
  <ReactLoading
    type={"bars"}
    color={"rgba(0, 0, 0, 0.54)"}
    className="statistics__spinner"
  />
);
